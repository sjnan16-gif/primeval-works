# Primeval Works project memory

This is the canonical, living contract for the mod. Read it before every edit. Update it whenever a decision changes. The deeper design, backend, art, writing, UI, testing, and detail contracts linked at the end remain binding; this file records the decisions most likely to be forgotten or regressed.

## Project and platform

- Workspace: `C:\Users\Hayro\Downloads\primeval-works`. The old `nnnnn (1)` folder is unrelated and must never receive Primeval Works changes.
- Minecraft 26.1.2, NeoForge 26.1.2, Java 25, GeckoLib 5.5.x.
- Namespace `primevalworks`; Java package root `com.primevalworks`.
- The server owns gameplay state. Clients send intent and render synchronized results.
- One shared dinosaur entity implementation serves all species. Species data supplies balance and visual profiles.
- One Command Table per player. Different players' tables need at least 72 blocks between them.

## Core game

- The fantasy is a smaller, highly controllable dinosaur base-automation game: hatch companions, equip a crew, automate the base, defend it, upgrade it, and explore for progression materials.
- Five specialties: Transport, Fire, Energy, Crafting, Expedition. The older Gathering job is now Expedition.
- Specialty rating is zero to four stars and is primarily species-defined. Every dinosaur can attempt every specialty, but bad off-specialty work is capped at 45% and a zero-star role may be unavailable where the activity needs specialist competence.
- Seven active crew slots are available first. Base upgrades unlock a second page up to fourteen total active slots.
- Every active companion has one authoritative command mode. `Home` returns to base duty, `Stay` holds the chosen area while still allowing urgent eating, and `Follow` travels with the owner and enables field work. The companion screen uses one button that cycles Home -> Stay -> Follow -> Home; never restore three separate command buttons. Hover copy explains the current mode, next mode, and follower-cap lock.
- A player begins with one follower slot. The two-rank Field Command base upgrade adds one slot per rank, for a hard maximum of three. Only active, living, non-expedition companions count; depot/storage transitions normalize to Home, and reload/activation must force any invalid excess followers back to Home.
- Combat-capable followers defend their owner and join attacks against the owner's live target. Non-combat species still follow but never gain fake attack capability.
- The Dino Whistle is the field-automation controller. Right-clicking either its player-inventory slot or the held whistle immediately opens a readable `196x145` authored-bubble setup screen; there is no Shift hold, progress hold, permanent world HUD, or right sidebar. Its rows, inset text spaces, rules, range control, catalogue paper, and local slots are cropped from the native `427x240` `ui_crop.png` atlas through `PrimevalUiCrop`; the marked-world follower picker retains `hotbar.png`. Text stays normal-sized and bold with wrapped descriptions, spring opening, restrained parallax, and settled hover motion. Quarry and Lumber consume left-click input and open the live-follower picker: Quarry toggles between a same-block Connected vein and a two-corner Area that clears every safe block inside, while Lumber marks one connected tree. Harvest and loose-item Collect are automatic follower-centered duties. Collect opens a `198x128` copy of the Work Planner's creative-style item catalogue: a plus target slot, main-panel-left/search-panel-right slide, spring results, searchable 9x4 registry grid, scroll paging, and click-or-drag filter selection. There is no Once/Loop control. The configured follower leash/search range is 16 through 85 blocks.
- Field work is server-authoritative and follower-only. The server revalidates the whistle, ownership, active crew, follower cap, command mode, dimension, player-to-mark reach where applicable, target type, protection event, `mobGriefing`, hardness, and area bounds. It never force-loads chunks or breaks block entities or Primeval Works machines. Connected work remains capped at 64 blocks; marked Quarry grows from 64 blocks and a six-block span at level one to the hard 512-block and sixteen-block limits at level 100. Marked Quarry and Lumber clear on the same tick as their final completed target; automatic Harvest/Collect rescan around their follower. Every unfinished assignment survives NBT reload, chunk unload, logout, depot storage, and temporary Home/Stay modes. Home/Stay suspends field execution without erasing the order; returning the same dinosaur to Follow resumes it.
- Follow pursuit and field execution never issue competing paths in the same tick. Automatic workers catch the owner, take one bounded crop/item excursion, return cargo where applicable, then resume formation; marked Quarry/Lumber owns navigation while the owner remains inside the configured leash around the mark. Active Follow is field duty and overrides automatic night sleep. A swimming Spinosaurus uses smooth direct aquatic catch-up rather than its idle surface-float goal, and short loose-item retrieval uses a direct collision-safe approach instead of waiting forever on a failed ground path.
- Field work is a single hidden specialty, not four extra ratings on every dinosaur. Exactly five current playable specialists receive it from one of their highest base jobs: Velociraptor/Transport -> Ground Retrieval, Tyrannosaurus/Fire -> Quarrying, Spinosaurus/Energy -> Quarrying, Parasaurolophus/Crafting -> Lumbering, and Dodo/Expedition -> Field Harvest. Triceratops, Stegosaurus, and Pteranodon have no field specialty. The Jobs screen shows one authored-bubble, normal-sized field row and assignment still requires Follow plus the whistle.
- Hatching before a Command Table is valid: owned dinosaurs remain saved and can join the first claimed base later.
- Dinosaurs have hunger, mood, health, level, birth quality, random scale, and slight procedural hue variation. Hunger, mood, health, jobs, expeditions, cargo, recovery, genetics, saddle state, and ownership must persist through chunk unload, logout, restart, depot moves, and death.
- Food Box: below 50 hunger, an available dinosaur stops safely, eats valid species food from the box, and refills. Diets are species-aware; large carnivores do not eat berries.
- Work may request hunger repeatedly, but actual work hunger loss is capped by one persisted species-sized drain clock. Completing several short actions in the same interval must never multiply the drain or turn automation into constant feeding.
- Dinosaurs sleep at night unless assigned to a night shift. Night work drains mood/sanity 130% faster and the assignment UI must warn the player.

## Genetics and mutations

- Only two mutations exist: Huge and Albino. Both may occur on one dinosaur and both icons/names must be shown.
- Huge: 18% larger rendered model and collision, +20% work/movement/combat stats, additional health/damage benefits through the shared mutation multiplier, and mount attachment must scale exactly once.
- Albino: rarer; preserves texture shading while shifting the body toward white and makes the pupils authored red. It buffs work, movement, and mount speed by 40% but lowers health. Albino hatch quality trends only slightly higher.
- Fossil Fragment restores original pigmentation only. It does not remove the Albino mutation or its stats.
- Red pupils are not a global red tint. Every authored species needs an explicit pupil mask/texture treatment so only its pupils become red. Current authored dinosaurs to audit: Dodo, Tyrannosaurus, Velociraptor, Pteranodon, Stegosaurus, Parasaurolophus, Spinosaurus.
- Breeding requires two Nesting Treats and two owned dinosaurs of the same species. Bred eggs have slightly stronger quality/mutation odds and inherit parental mutations more often.

## Ownership, depot, expedition, and recovery state machine

Every dinosaur UUID must be in exactly one authoritative state:

1. `ACTIVE_LOADED_OR_VANILLA_SAVED`: selected in an active crew slot; exactly one world entity may exist.
2. `DEPOT`: snapshot only; no world entity.
3. `EXPEDITION`: snapshot/timed state only for UI and rewards; no second active world entity may spawn.
4. `RECOVERING`: deceased-row snapshot with a recovery deadline; not active and no world entity.
5. `PERMANENTLY_REMOVED`: removed only when its owner intentionally kills it by normal player damage; it drops the defined dinosaur materials.

Rules:

- Command/admin `/kill` is a test defeat and must send an owned dinosaur to `RECOVERING`, not leave a roster ghost. Ordinary hostile damage also recovers it.
- A recovering dinosaur must be removed from active IDs before the roster payload is sent. It stays visible only in the four-slot deceased/recovery row until its timer completes.
- Recovery completion restores full health before activation/spawn and clears stale fall velocity/distance.
- Expedition dinosaurs cannot be moved, swapped, recalled, or spawned until their expedition finishes. Rejoin/reload must reconcile an expired expedition once and must never duplicate it.
- Depot/recall/expedition/recovery operations are transactions: capture the newest snapshot, remove the old world authority, update roster and active indexes once, then synchronize.
- Cargo is returned safely before depot, expedition, recovery, or reassignment. Failure to insert drops marked base cargo at the Command Table rather than deleting it.

## Global dinosaur animation and AI contract

- Authored idle, walk, run, work, sleep, swim/fly/glide, hurt, attack, and special clips remain the primary animation. Procedural motion augments them; it must never replace or quantize them.
- Movement animation speed follows real movement speed. Slow walk plays slowly; sprinting and speed mutations play the same authored gait faster without foot sliding.
- Action and locomotion are layered: attacking/working may animate the upper body while moving legs keep the current walk/run cycle. Never freeze moving legs because an upper-body action began.
- Turning is procedural and smooth. Head/neck looks toward the destination first, torso follows with spring-like lag, then hips/body align. Moving dinosaurs keep locomotion; a separate authored turn clip is not used.
- Large dinosaurs turn deliberately but may not moonwalk, spin 360 degrees, walk backward toward a target, or enter a target's body. They maintain mouth/contact distance and face a target before attack contact.
- Attack damage occurs at the authored contact frame, not at animation start. While committed to an attack, the dinosaur looks at its target. Mounted attacks look toward the rider's attacked point.
- Idle dinosaurs roam within their base, observe nearby activity, and occasionally look around. Their idle roam roll is one in thirty eligible AI checks: present without turning the base into constant movement. They look directly at a player only within roughly three blocks and not while attacking, working, or sleeping.
- Hitboxes and step height follow the authored true model scale plus genetics. Do not arbitrarily shrink a species to make previews fit.
- Heavy dinosaurs are not pushable and take negligible knockback. Hostile mobs and combat-capable owned dinosaurs target each other within the base.
- Sleeping uses a simple vanilla-style `Z` billboard sequence scaled to the dinosaur.
- Heavy dinosaurs may begin sleeping only when the ground under their footprint has a stable, flat 3x3 support area. Do not let a large sleep pose balance on a single block or ledge.
- Work progress billboards appear above the workstation, not above the dinosaur. Cargo uses the authored hotbar-slot frame above the carrier and the item also appears at the mouth attachment where supported.
- Work approach distance is species-sized and collision-aware. A large dinosaur works from the nearest clear contact distance; it must not force its collision box through the workstation just to satisfy a center-point distance check. Nonlethal owner hits cancel combat and allow assigned work to resume.
- One saved work order may contain from one through the configured maximum number of compatible workstations (eight by default). Every target must pass the same base, ownership, and block-type validation; energy stations also remain one-worker-per-turbine. The dinosaur persists a round-robin cursor and advances it after successful cycles so a valid multi-target order does not silently work only its first block.
- Velociraptor pursuit steering has one authority at speed: route direction is blended continuously into existing momentum, while the generic delayed body-facing velocity correction is skipped. Transport navigation is not restarted every ten ticks while its path is already live. Do not restore either behavior; the two corrections fighting each other produced the visible left-right running zigzag.

## Spinosaurus contract

- Spinosaurus is a top-tier large dinosaur: four-star Energy and Expedition, very high health/damage, long mouth-scale attack reach, strong water travel, and a comparatively fast authored walk. Its land walk clip is normally about 30% slower than the export, then scales continuously with actual land speed.
- It is saddle-gated. The rider must follow the authored `whereplayersits` locator, including its swim offset; never substitute `head2`, the middle of the back, or a static world-space render offset. Runtime applies one three-model-pixel downward support correction after the animated locator, and Huge scales the complete attachment/correction exactly once.
- On land: normal walk plus Shift+forward sprint, clear acceleration, stronger FOV at sprint, two-block mounted step traversal, front/rear terrain-sampled pitch over rises/descents, and a brief strong distance-aware camera impulse at each authored foot contact. The rider stays attached through steering and terrain pitch.
- Land sprint has its own synchronized 100-point stamina pool. Sprint drains it server-side, exhaustion cancels sprint until it has recovered past the restart threshold, and a compact HUD bar appears only while riding on land. Swimming/breaching never consumes this land-sprint stamina.
- Space never makes a land-mounted Spinosaurus jump. Normal block traversal comes from its step height and smoothed terrain pitch.
- In water: smooth acceleration, pitch/yaw/bank steering, no stamina, and rider oxygen drains ten times slower. Do not add Night Vision or custom underwater fog/clarity.
- Unmounted water behavior is separate from mounted swimming: a Spinosaurus uses restrained surface buoyancy and may bob at the waterline, but never inherits vanilla `FloatGoal` jump impulses, launches most of its body into the air, or holds the swim animation after leaving water. This rule must not alter mounted swim/breach controls.
- Underwater mounted attacking is disabled. A land mounted left-click attack uses the authored Spinosaurus attack and turns head/body toward the attacked point; it must never hit its own rider or mount.
- Breach: depends on swim speed and exit angle, stays in swim animation through the arc, turns down naturally, damages contacted external entities, and never damages the Spinosaurus through its own fall/landing.
- Spinosaurus UI previews must fit the exact same rectangle as every other species. Fix preview projection/bounds, never world/model scale.
- Unmounted Spinosaurus begins its bite during the final approach and facing correction. It keeps mouth distance and turns through the short authored wind-up instead of rushing to a hidden stop point, standing still, and only then starting the attack.
- The current Spinosaurus export is the authoritative model/animation set and has six authored 512x512 state atlases: open, blink, saddled-ground open/blink, and saddled-aquatic open/blink. Runtime names normalize the source export's `undergroundmount` typo to `spino_saddled_aquatic.png`; do not rename the runtime state or replace these atlases with generated substitutes.

## Command Table block

- The Command Table is exactly one block for placement, collision, selection, ownership, rendering, and item representation. There is no live extension block.
- Its authored block model and `dino_command_table.png` are the source of truth. Never replace it with a generated shape or scale it down/up to solve an inventory-render problem.
- It is as hard as obsidian.
- The screen has seven active slots per page, base actions, energy meter, skill tree, depot, and a deceased/recovery row. Store All must actually move all eligible active dinosaurs to the depot; dragging works both directions. Right-clicking an available active dinosaur opens one compact Call Back button that recalls only that dinosaur without changing crew membership, ordering, or automation.
- Main action-label offsets are authored UI coordinates, applied before panel scaling: Store All `(+4,-1)`, Recall `(+2,-1)`, Energy `(+1,-1)`, and Depot `(-1.5,-1)` pixels. These values correct where the labels were reported to be off; do not invert them again or move the button art/hitboxes.
- Base upgrades do not emit a screen-wide ripple. A purchased node gives a compact gold glow ring, settle, and level-up/chime sound. Only genuinely newly visible child nodes spring-slide outward from their parent; buying another rank or unlocking an already-visible node must not replay the extension. Upgrade requirements render as item icons inside the authored hotbar-slot frame, with text kept in a separate bounded region.
- Primeval Works audio is intentionally minimal. Screens use a restrained vanilla-backed layer for open, close, click, denied, and upgrade feedback; opening/closing must not spam page-turn or paper sounds. A drag beginning in empty skill-tree space is navigation, not an action, and must be silent. Gameplay adds only heavy-dinosaur footsteps and a continuous speed-reactive Elytra-style Pteranodon airflow loop while its rider is airborne. Dinosaurs have no ambient, alert, hurt, death, attack, eating, sleeping, or work cues, and machines, eggs, incubation, turrets, breeding, berries, and the Powered Observer add no triggered or looping audio. Heavy steps fire from authored GeckoLib contact keyframes, follow clip speed automatically, use a steep local falloff, and have a configurable 5.5-block default hard limit. The Pteranodon loop is rider-local and continuously follows flight speed. There is no custom sound registry or `sounds.json` dependency.

## Master configuration contract

- `primevalworks-client.toml` owns only presentation and local feel: UI sound, heavyweight step volume/range/dust/shake, Pteranodon wind, mount FOV/bank, stamina HUD, hatch card, and unmounted Spinosaurus gait speed. It is editable from Primeval Works in the Mods screen through NeoForge's native configuration screen.
- `primevalworks-server.toml` is the authoritative per-world/server balance file. It covers needs, recovery, breeding, mutation odds and multipliers, work/transport/energy, target limits, expeditions, mount movement/stamina, dinosaur combat, defenses, and machine power/speed. Dedicated-server owners edit the world server config; connected clients consume the synchronized values and never substitute their own gameplay balance.
- Config labels and descriptions are short human instructions, not internal field names. Gameplay rules read one immutable runtime tuning snapshot so pure unit tests remain independent of the NeoForge client/runtime and config reloads cannot expose a half-updated set of values.

## Command Table UI coordinate contract

All coordinates are in the authored 427x240 texture. They describe the usable interior; do not expand previews into the outline or the meters. Convert both rectangle edges through the screen transform and retain floating-point bounds until scissor submission.

- Active crew preview 0: `(115, 51)`, size `23x23`.
- Active crew preview `i`: x = `115 + 27*i`, y = `51`, size `23x23`, for seven slots.
- Depot living preview at column 0,row 0: `(311, 66)`, size `23x23`.
- Depot next row in the current authored sprite: `(311, 93)`, size `23x23`.
- Depot next column: `(337, 66)`, size `23x23`; x stride 26, y stride 27.
- Living-depot x stride is 26 and y stride is **27**, not the obsolete 33-pixel stride. The visible regression is diagnostic: row one fits, row two is 6 pixels low, and row three is 12 pixels low.
- Recovery/deceased preview 0: `(311, 155)`, size `23x23`; x stride 26 for four slots. Its interactive outer slot is `(309 + 26*i, 152)`, size `26x27`, and the label is below the row rather than inside a model window.
- Every entity preview is a top-down three-quarter view, fitted from species bounds to the rectangle. It follows the same smooth panel parallax with a slight cursor-reactive rotation.
- Preview models must be clipped to the transformed float rectangle. No part may leak even a fraction of a pixel into an outline, meter, neighbor slot, depot label, or outside panel during parallax, opening, hover, or dragging.
- Do not solve oversized Spinosaurus/Parasaurolophus/Pteranodon previews by changing entity size or hitbox. Correct their preview bounds/camera fit.
- All dinosaur-rendering UI, including the hatch reveal, uses the shared `DinosaurPreviewBounds`; never duplicate a partial species switch that silently treats Spinosaurus like a Dodo.

## Current content decisions

- The active jam roster is exactly eight species: Tyrannosaurus, Triceratops, Velociraptor, Stegosaurus, Parasaurolophus, Pteranodon, Dodo, and Spinosaurus. Brachiosaurus, Dilophosaurus, Ankylosaurus, and Pachycephalosaurus remain enum values only for decoding old ownership data; they have no entity registration, spawn egg, hatch command suggestion, advancement, or wild-egg entry. Legacy ownership records for one of those inactive species migrate to Dodo instead of exposing unfinished content.
- Current authored runtime models: Dodo, Tyrannosaurus, Velociraptor, Pteranodon, Stegosaurus, Parasaurolophus, and Spinosaurus. Triceratops remains the only active-roster placeholder until its authored assets arrive.
- The authored Velociraptor source is `art/entities/velociraptor/velociraptor.bbmodel`. Downloaded `raptor.*` exports are normalized to the runtime asset name `velociraptor`; its current compatibility bones are `head2`, `lowerjaw`, `rightleg`, and `leftleg`, and its supplied gameplay clips are `idle`, `walk`, `run`, `work`, `sleep`, and `attack`. Do not let it fall back to the Field Dodo model or placeholder animations. A future contract-clean socket/bone re-export must migrate source, runtime exports, and renderer mappings together.
- Current specialty stars in Transport/Fire/Energy/Crafting/Expedition order: Tyrannosaurus `1/4/1/1/4`, Triceratops `3/1/1/2/3`, Velociraptor `4/1/1/2/2`, Stegosaurus `1/3/1/1/3`, Parasaurolophus `1/1/3/3/1`, Pteranodon `3/1/2/1/2`, Dodo `2/2/1/2/4`, Spinosaurus `1/2/4/1/4`. The efficiency curve is `20/20/45/65/100%`; mutation and species-passive bonuses are displayed on top and may visibly exceed 100%.
- Species passives are unique and functional: Tyrannosaurus improves fire tending, Triceratops carries extra cargo, Velociraptor builds pursuit momentum only on real transport routes and while hunting, Stegosaurus accelerates fire work, Parasaurolophus reduces same-owner nearby work-mood drain, Pteranodon improves long-route transport flight, Dodo improves expedition outcomes, and Spinosaurus improves energy generation. A Velociraptor uses its original slow walk for roaming, eating, returning to base, and every non-transport job; those states must never build speed or select the run clip. Passive strength scales with birth quality, level, Huge, and Albino; a passive aura never applies between ownerless dinosaurs or across owners.
- The Ancient Reforged Bayonet is removed from playable content. Its retired item ID is an invisible save-compatibility alias to the Primordial Sword so older inventories do not gain a missing item.
- Saddles: Pteranodon Saddle and Spinosaurus Saddle. No Tyrannosaurus Saddle.
- Machines/blocks include Command Table, Food Box, Processor, Ancient Furnace, Premium Egg Incubator, Water Turbine, Wind Turbine, Powered Observer, Dart Turret, Laser Turret, Ancient Spell Stone, Ancient Barrel, the placeable Spinosaurus Head trophy, and related progression blocks/items in the content catalog. Both reinforced piston variants were cut completely before release rather than retained as aliases.
- The old `raw_ancient_spell_ingot` and `ancient_spell_ingot` registry IDs are compatibility aliases only. Live progression uses `magic_shard_fragment` from Primordial Frontier expeditions and refines it with a Fossil Fragment in the Processor into the animated `magic_shard`. Ancient Metal also has an authored nugget with reversible 9:1 crafting.
- Removed/not planned: Dino Crafter, Cooker/Cooking Station, Extended Piston, Sticky Extended Piston, Enhanced Rail, Ancient Reforged Bayonet, Water Wheel, Ancient Chest, Ancient Chest Opener, Super Chest, energy dust. The old `enhanced_rail` and `ancient_reforged_bayonet` registry IDs remain only as invisible save-compatibility aliases.
- Processor makes reinforced/compressed outputs and requires heavy base power plus its authored three inputs/output workflow. It also upgrades a basic Wind Turbine with one Pteranodon Wing Fragment. Compressed metal/core is not a normal expedition reward.
- Ancient Furnace uses base energy instead of fuel and has a 2.5 E/s minimum-to-slow setting through a higher-cost maximum producing up to 4.2x speed.
- The authored Ancient Furnace progress fill is cropped from `furnace_fill.png` into `(118,112)`, size `6x21`; it communicates the current smelt cycle without adding a replacement code-drawn meter.
- Ancient Barrel is a 54-slot directional container using the authored 64x64 closed/open atlases from `art/blocks/ancient_barrel`. Its vanilla block JSON UVs are normalized to the 0-16 model range, it switches its `open` blockstate while viewed, and its closed model is also the inventory preview.
- Wind Turbine has two persistent phases using the same authored model: the craftable basic phase produces 60% of the former output with `wind_turbine_base.png`; the Processor turns it into the full-output Upgraded Wind Turbine using one Pteranodon Wing Fragment and `wind_turbine_upgraded.png`. Both phases remain valid Energy workstations after save, break, place, and assignment. Wing Fragments appear only in Predator Run/Primordial Frontier expedition pools, alongside the direct Pteranodon drop. The rotor is still unless a valid assigned worker is presently generating at that exact turbine.
- Water Turbine is a 3x3, one-block-thick animated cog. Every structure cell can retain water, but generation only requires its bottom row of three cells to be waterlogged. A valid submerged turbine contributes passive power at 20% of its normal 1.5x output to the nearest loaded Command Table in range and rotates slowly. Assigning an Energy worker adds the full worked output and runs the rotor at work speed. One directly worked cog couples at most its two nearest valid Water Turbine neighbors within five blocks for 55% and 35% output. Coupled turbines animate, cannot recursively couple, and a turbine with its own worker is never borrowed by another cog.
- Companion navigation uses continuous turn-speed scaling instead of stop-start yaw gates. The head leads a turn, the body follows without freezing the path, idle roaming is less frequent, and ambient observation lasts longer. Follow mode runs at distance, uses species travel (Pteranodon flight and Spinosaurus swimming), and may only emergency-teleport at 100 blocks or farther to a collision-safe position.
- Quarry area selection is a true two-stage order: the first corner only starts a green region preview; the second corner completes it and opens follower selection. The chosen region remains faintly visible while the whistle is held. Call Back is a separate server-validated command that immediately recalls the dinosaur to its table and changes its mode to Home.
- Dart Turret has an internal 3x3, nine-slot dart magazine, consumes real Dart items, requires assigned base energy, automatically locks hostile mobs, and fires the authored Dart as a crossed-sprite arrow-style projectile. Its inventory panel uses the same warm primitive palette as the Food Box. The authored Dart texture is projectile art only; its temporary inventory icon is vanilla until a separate icon is supplied. Laser Turret is the expensive technology defense upgrade: it consumes 5 E/S only while it has a target, needs no ammunition or worker, has no right-click screen, and delivers four 5-damage laser contacts five ticks apart before a one-second recovery. Its custom damage type bypasses the normal hurt cooldown so all four synchronized contacts land; a burst remains locked to one target and never jumps midway to a different mob. Its source is `art/blocks/laser_turret/laser_turret.bbmodel`; the runtime uses the supplied 128x128 idle atlas, six-frame firing sheet, and `base/head/mouth` Gecko hierarchy. The base stays fixed, the head follows synchronized target yaw/pitch, the jaw and firing sheet animate only with a live target, and the red/white geometry begins at the authored snout. The recipe consumes a placeable Spinosaurus Head, two Redstone Blocks, two Compressed Ancient Metal Ingots, two Ancient Metal Ingots, a Compressed Core, and a Laser Observer. Red/white particles appear only as brief muzzle and impact feedback; the beam itself remains synchronized geometry. The canonical block/item/block-entity ID is `laser_turret`; retired `magic_turret` saves migrate to it. Powered Observer projects a thin, translucent two-color red five-block detection beam with a small red endpoint sparkle and emits observer-style redstone on updates inside that beam. Its line is persistent emissive square-prism geometry with a subtle travelling core pulse, never a dense particle chain; only the endpoint remains a sparse particle.
- The Spinosaurus Head trophy is the Laser Turret source model's complete `head` and `mouth` geometry, using the turret's authored 128x128 atlas. Its exact turret dimensions are preserved, but the geometry is grounded at block-floor height and uses a matching directional collision shape so placement behaves like a real trophy block. A player-performed Spinosaurus kill has an 8% trophy chance. Primordial Frontier has a separate 1.8% rare-find roll; lower expeditions never award it. Breaking the placed trophy returns the block.
- Laser Turret and Powered Observer sight is collision-bound. A Laser Turret only acquires and retains a hostile with a clear block sightline; closing a wall cancels the remaining burst before damage. The Powered Observer ignores updates behind the first colliding block. Both renderers clip their geometry and endpoint feedback at that same first collision.
- Connected active machines request power only while they have real work. An empty Ancient Furnace, invalid/blocked Processor, empty Incubator, or idle turret must not drain the base reserve. A connected turret acquires a hostile before requesting its rated power, then fires after the server ledger grants it; lack of power pauses its cooldown instead of resetting it.
- Wild eggs are world blocks in small, medium, and large weighted pools. Small rolls Dodo/Pteranodon/Velociraptor at `54/32/14`; medium rolls Parasaurolophus/Triceratops/Stegosaurus/Pteranodon/Velociraptor at `32/26/22/13/7`; large rolls Tyrannosaurus/Spinosaurus/Stegosaurus/Triceratops/Parasaurolophus at `32/28/17/14/9`. The worldgen chance denominators are `24/72/180`, making every egg rarer than before and larger eggs progressively rarer. Right-click hatches into ownership and leaves one 1-3 Fossil Fragment stack; larger eggs have progressively better second/third-fragment odds. Silk Touch collects the block for incubation. Incubation is in-world with a timer, not a menu.
- An egg inside the Premium Egg Incubator fills the glass chamber according to its small/medium/large model height. It may settle and rock almost imperceptibly, but it is not a tiny rotating display item.
- Expeditions have five authored risk/reward tiers and fixed possible reward pools. The Primordial Frontier is unavailable at one star; its unmutated risk curve is exactly 92% at two stars, 64% at three stars, and 12% at four stars. Its base duration is 120/64/34 minutes for two/three/four stars, and a four-star dinosaur with both mutations finishes in roughly 18-22 minutes before Command Table upgrades. Mutations reduce both duration and incapacitation risk through the same synchronized server/UI multiplier. Expeditions do not award meat/bone. Ancient metal/core/silk/sulfur and other non-craftable progression materials belong in suitable tiers; compressed materials are processor products except an intentionally rare high-tier compressed core reward.

## Recurring regressions: never reintroduce

- Command Table visual replaced, rescaled, made two blocks, or left with a two-block hitbox.
- Command Table custom entity renderer registered over the authored vanilla block model. The current table is a one-block vanilla model using `models/block/command_table_world.json` and `dino_command_table.png`; do not register the old Gecko renderer for it.
- Command Table action labels lose their individual authored offsets, upgrade branches teleport or replay on later ranks, a screen-wide ripple returns, or requirement copy draws beneath requirement items. Preserve the tested label offsets, one-time parent-to-child reveal slide, localized node glow, and slotted cost layout.
- Large species preview leaks out of a UI slot, is made tiny, or changes world hitbox/scale to compensate.
- UI scissor rounds before applying animation/parallax, causing half-pixel leaks.
- Deceased model/label uses living-depot coordinates or an entity remains active after entering recovery.
- Command `/kill` bypasses recovery and leaves a roster entry with no world entity.
- Recovery GameTests spawn a recall entity outside the forced-ticking test region, leaving it at `tickCount=0` and producing a false lifecycle failure. Recovery tests must force-load both the Command Table and dinosaur positions before invoking defeat.
- A saved expedition spawns once from active state and again from its expedition snapshot after login.
- Login searches only currently loaded entities, so an unloaded active crew appears missing or loses its visible automation until the base chunk is revisited. Login must load the snapshot's last chunk plus the bounded base area, restore saved active IDs only, preserve their ordering and full work order, and never auto-fill empty slots from depot records. Spawn fallback uses the claimed Command Table's dimension, not whichever dimension the player logged into.
- An owned dinosaur reaches lethal damage while its owner logs out, the recovery transfer fails to write the offline player's data, and the entity dies or remains a roster ghost. Persist a pending-owner-recovery state at one health, retry when the owner returns, then perform the normal single recovery transaction.
- Mutation/hue/saddle/health/work state differs between live entity, depot card, expedition card, and recovery card.
- Full health renders partially filled because the UI compares saved health against a newly recomputed max incorrectly.
- Moving attack/work cancels the legs' locomotion controller.
- Large predator turns snap, twitch, moonwalk, or rotates its body before its head.
- A mount uses a static body offset rather than its authored animated attachment, especially after Huge scaling.
- Spinosaurus logical and rendered rider sockets disagree on the forward-axis sign, use `head2` instead of the authored `whereplayersits`, or let the animated socket expire and briefly fall back to the vanilla middle seat. At yaw zero the logical seat is positive Z; camera/avatar rendering always uses the persistent seat sample or the same forward fallback, never vanilla placement. Ground samples are immediate; only aquatic samples receive a low-latency stability filter.
- Pteranodon ground state accidentally enters flying animation/stamina behavior.
- Spinosaurus breach or landing applies fall damage to itself.
- A mounted Spinosaurus snaps to terrain pitch, exceeds a subtle 12-degree incline/decline lean, or loses its ground-sprint horizontal momentum when crossing a one-block drop. Momentum preservation is limited to a short forward step-down grace window and must never turn into cliff gliding.
- An unmounted Spinosaurus uses vanilla `FloatGoal`, repeatedly jumps out of deep water, or remains in its swim pose while airborne. Use the species-only surface buoyancy goal; never alter mounted swim physics to solve this regression.
- A small dinosaur can deadlock beside a workstation when center-to-center range is used after navigation stops. Contact checks must intersect the workstation cube against the dinosaur collision box inflated by its species work reach, and pathing should approach the block's ground-level work point.
- A Parasaurolophus mood aura treats two `Optional.empty()` owners as equal and leaks across tests/bases. Require an actual matching owner UUID before applying any companion aura.
- A Pteranodon can have stale `onGround` contact for the first lift frames and immediately cancel a valid takeoff. Keep a short takeoff-only grace window; never use it to mask genuine landing contact later in flight.
- Machine assignment accepts the wrong block/slot, loses assignments on reload, or duplicates a worker reservation.
- Multiple workers at one work target render multiple progress icons instead of one faster combined indicator.
- Transparent placeholder items are too bright or can be grabbed as real items.
- Repeated work completions each drain hunger and bypass the species interval. Hunger has one persisted drain deadline shared by every work specialty.
- Albino recoloring bleaches authored Pteranodon/Spinosaurus saddle pixels or a saddled/albino state stops blinking. Equipment pixels are detected from the authored plain/saddled atlas pair and copied unchanged after recoloring; every open/saddled/aquatic atlas keeps a matching blink state.
- A rapid Laser Turret burst uses ordinary hurt invulnerability and only its first contact deals damage, or a saved burst transfers its remaining contacts to a different target. The damage type bypasses cooldown and the persisted target UUID owns the complete burst.
- The removed Enhanced Rail reappears in creative content, recipes, previews, UI copy, or progression. Only its block/item save alias may remain.
- The removed Bayonet reappears in creative content, recipes, models, UI copy, combat hooks, or animation mixins. Only its item save alias may remain.
- A vanilla Laser Turret block model renders beneath the authored Gecko model, producing a doubled pedestal/head, or the complete authored model rotates as one rigid piece. The world render shape stays invisible; the Gecko `base` is fixed while only `head`/`mouth` follow the synchronized target. Shot particles are brief feedback only and never define hit position or timing. Its inventory form is deliberately a normal vanilla block item containing all 23 authored cubes and the real turret atlas; do not restore either the tiny special renderer or the old generic-texture placeholder.
- Powered Observer reverts to a dotted particle line, renders a turret-thick beam, or culls when its five-block endpoint leaves the observer block's own bounds. Keep the dedicated observer block entity renderer, thin nested red prism layers, expanded directional render bounds, travelling core pulse, and endpoint-only sparkle.
- A saved powered consumer silently stops after a chunk/world reload because its transient consumer-to-table binding was lost. Loaded Command Tables are indexed by position; `BaseEnergyRules.isPowered` must self-heal from the table's persisted enabled-consumer set. The powered-turret/base-energy GameTests deliberately clear the transient binding and require immediate recovery.
- Velociraptor combat pauses before contact, snaps into the attack, or discards its chase velocity. Pursuit momentum is continuous movement state; the pounce blends the current heading toward the target, launches without a pre-attack stop, keeps locomotion under the action clip, and applies damage at the authored contact frame.
- Velociraptor Pursuit Instinct leaks into ordinary navigation. Only an active item-transport route, live combat target, or committed pounce authorizes acceleration and the run animation; idle roaming, hunger travel, base return, and other specialties remain at the authored walking pace even if navigation is active.
- A follower increases navigation speed to catch its owner while the visible gait stays at one fixed rate, producing sliding feet. Pteranodon, Spinosaurus, and Velociraptor retain their specialized flight, swim, and pursuit curves; every other following species scales walk and run playback from Minecraft's smoothed movement state, with bounded rates and normal non-follower timing left unchanged.
- Birth quality becomes decorative. It has a deliberately small effect on work duration, hunger interval, and work-mood drain, while quality, level, Huge, and Albino all scale the species passive shown in the companion UI.
- The Work Planner left rail is anchored to the centered 427-pixel canvas and floats away from the physical screen edge on a different aspect ratio. Only that rail uses physical-screen edge anchoring; the authored center, right, top, and bottom pieces keep their existing canvas transforms.
- Idle connected machines drain the energy buffer, and turrets gate target acquisition on that depleted buffer, creating a permanent no-target/no-power deadlock. Active consumers expose whether they have real work; the ledger sums active demand, while turrets acquire from an enabled connection before checking their granted power. The powered-turret GameTest waits with no targets, requires an unchanged reserve, then independently requires both turrets to acquire and strike.
- A species center-to-center work reach is reused as raw collision-box inflation, leaving large workers several blocks farther away than intended. Convert center reach to a collision-gap allowance by subtracting half the dinosaur width and half the workstation width before inflating.
- A worker path ends against a workstation just outside its interaction threshold and silently holds cargo forever. Its collision-aware approach clearance and its interaction reach must overlap the navigation stopping tolerance; repeat the Processor round-trip test when either value changes.
- A base inventory scan crosses into a second Command Table's storage when tables exist through commands, migration, or test setup. Every indexed container belongs to its nearest loaded Command Table, with a stable position tie-breaker.
- A turret spends power on or attacks a hostile whose nearest Command Table is another player's base. Dart and Laser targeting must remain inside the connected table's radius and nearest-table cell.
- A fluid/entity GameTest spawns outside a forced-ticking chunk and reports frozen spawn velocity as real movement. Force-load every entity and machine position whose ticks are part of the assertion.
- Concurrent combat GameTests let normal hostile acquisition replace the test's intended target. Tests for attack timing/range must keep their own target assigned; separate tests cover autonomous target selection.
- Gameplay sound creep returns after an audio-polish pass. Do not attach generic vanilla cues to every machine/action: the deliberate whitelist is UI feedback, heavyweight footsteps, and mounted airborne Pteranodon airflow only.
- Heavy-footstep distance measured to a large dinosaur's origin makes a rider sound far away while mounted. Measure from the listener to the nearest point of the collision bounds, retain the 5.5-block hard cutoff, and tune strength through the local falloff rather than extending the audible range.
- A Water Turbine replaces water with dry multiblock parts and then validates unrelated front/back water cells, so an apparently submerged machine never starts. Master and part states must remain waterloggable; the authoritative validity rule is exactly the three waterlogged bottom-row cells, covered by the turbine structure GameTest.
- Observer or Laser Turret gameplay obeys walls while its client geometry still draws through them, or the inverse. Keep collision sight shared by server targeting/detection and client beam length, and retain wall coverage in both defense GameTests.
- A newly authored species is copied under its download nickname but the runtime profile still points at the registry species ID, so it silently renders the Field Dodo placeholder. Normalize every source/export path to the stable registry ID, add an explicit visual profile and preview bounds, route every supplied animation, and keep the presentation-asset test beside the import.
- Thin exported turbine panels disappear or show an untextured corner because their authored two-sided planes use a culling render pass. Dinosaur and solid turret geometry keeps alpha-cutout back-face culling, while Wind/Water Turbine block and item renderers use alpha cutout without face culling so both authored faces remain visible. Transparent defeat/beam effects stay in separate passes.
- Every block item is forced through one full-cube transform, shrinking trophies/turrets and making animated turbines fall back to primitive placeholders. Full cubes inherit Minecraft's `block/block` transforms; reviewed non-cube items use dedicated vanilla-style presentation templates, and Wind/Water turbine items render their exact authored Gecko geometry in a still state. Item sizing never changes the placed model, collision, or active world animation.
- A correctly scaled Gecko item still sits off-center because its authored origin, transparent texture silhouette, and Minecraft's GUI projection do not share a visible center. Basic Wind, Upgraded Wind, and Water each keep an independently verified GUI-only offset; never assume two atlases on the same geometry have the same visible center, and never move the world model or held pose to repair a hotbar issue.
- A Wind Turbine spins from environmental validity alone, or a Water Turbine runs at full work speed without a worker. Wind rotors require the exact station's short worker heartbeat. A valid submerged Water Turbine is the deliberate exception: it rotates at 20% speed for passive current, then switches to full speed only while directly or mechanically worked. Invalid or unloaded turbines stop.
- Energy assignment outlines fall back to obsolete turbine-part cubes instead of the authored complete machine silhouette. Wind highlights cover the full 3x4x1 model and Water covers the full 3x3x1 model, rotated from the master block's facing; no part-block mosaic should be drawn.
- A marked Quarry or Lumber order breaks its last target but remains visibly active until another entity tick. Clear the persisted marked order immediately after the last target resolves. Only automatic Harvest and Collect remain assigned for bounded rescans.
- A field-work GameTest occasionally reports an idle follower because its dinosaur, table, or marked block landed outside a forced-ticking test chunk. Force all three positions, keep the fixture invulnerable from unrelated parallel combat, and include command/field/action/entity-tick state in timeout diagnostics.
- A client forges a whistle assignment, selects a reserve/excess follower, reaches through unloaded chunks, or bypasses protection with a stale picker packet. Assignment packets are intent only and must repeat every ownership, active/follower-cap, state, reach, area, block, game-rule, and break-event check on the server.
- Follow/field state or carried loose cargo survives the wrong lifecycle boundary. Command mode and complete field order persist through ordinary reload, while depot, Store All, swap, expedition, recovery, and leaving Follow settle cargo and normalize/clear state exactly once.
- Follow and field work both own navigation, repeatedly cancel each other's route, and leave a companion standing still. Follow owns catch-up only when no bounded field target/cargo leg is active; an active passive excursion owns its target and then hands control back. Swimming Spinosaurus catch-up bypasses idle surface buoyancy, active Follow never starts scheduled sleep, and compatible passive specialists are filtered and revalidated server-side. Keep the land-follow, swim-follow, passive-worker catch-up, passive Harvest, and filtered Collect GameTests together.
- The Jobs screen grows a tiny sixth row containing four ratings and a paragraph, darkens that row into a separate visual system, or Home/Stay/Follow return as three separate buttons. The field row uses the authored warm bubble, normal two-line job typography, aligned stars, a species-colored hover state, settling text wobble, and one real specialty (or a clear unavailable state); the command control remains one cycling button.
- A Dino Whistle surface replaces the authored `ui_crop.png`/`space.png`/`hotbar.png` frames with raw option cards, restores Shift-hold configuration or a permanent world/right sidebar, adds Once/Loop, or lets attack fall through to a vanilla punch. Right-click immediately opens the `196x145` authored setup from either a player-inventory slot or the held whistle. Rows use nested bubbles and readable wrapped text rather than tiny floating labels. Quarry and Lumber mark the world; Harvest and Collect are automatic. Collect's `198x128` filter copies the Work Planner creative-catalogue language and accepts click or drag into the plus slot. Follower choice for marked work is not another dashboard: up to three authored `hotbar.png` slots pop over the mark and render clipped live previews. Legacy Quarry `Single` normalizes to Connected; Area remains Area; Lumber normalizes to Connected. Unit UI contracts plus marked Connected/Area Quarry, land/swim Follow, passive-worker catch-up, passive-Harvest, passive-Collect, and follower-reload GameTests cover this state.
- A crop-atlas control samples the whole sheet, leaks a neighboring sprite, or scales its border into soft/uneven pixels. `ui_crop.png` stays exactly `427x240`; `PrimevalUiCrop` owns every source rectangle, alpha-tinted region blit, and border-preserving nine-slice. Whistle and Work Planner catalogues call that shared helper rather than duplicating approximate colors.
- Quarry behaves as an automatic ore scanner in one layer but as a marked block tool in another. `FieldMode.requiresMark()/isPassive()` is the single policy source: Quarry and Lumber require marks; Harvest and Collect are passive. Connected Quarry follows only the marked block type, Area Quarry clears every safe breakable block between its two corners, and both are one-shot bounded orders. `DinoFieldWorkRulesTest`, `follower_quarries_marked_vein`, and `follower_clears_marked_quarry_area` keep UI, packet validation, and server execution aligned.

## Required verification gate

- Run unit tests and `gradlew.bat build` for every code/resource change.
- Run targeted GameTests for ownership lifecycle, `/kill` recovery, active/depot/recovery exclusivity, expedition reload, mutation/saddle snapshot parity, one-block Command Table, mount self-hit protection, rider attachment scaling, work persistence, energy uniqueness, and machine slot automation.
- Start an integrated client and a dedicated server. A clean startup is not proof of play behavior; manually exercise the changed gameplay before calling it verified.
- Check `logs/latest.log` and crash reports after any reported crash or disconnect.

## Last verified checkpoint

- Command Table world rendering uses the authored one-block model again; its collision/selection is one full block and its hardness remains obsidian-class.
- Active, depot, and recovery previews use the exact authored rectangles above with inward float-to-scissor rounding. Spinosaurus has explicit preview bounds; world scale and collision are never altered to fit UI.
- Owned dinosaurs defeated by hostile damage or `/kill` enter recovery, are removed from active IDs, and do not leave a roster ghost. Intentional normal player kills remain permanent and drop species materials.
- Spinosaurus mounted underwater attacking is disabled. Land mounted attacks aim from the rider look direction, unmounted attacks keep their target in view, the rider follows animated `whereplayersits`, breach landing does not self-damage, terrain pitch is smoothed, and land gait speed follows real movement speed.
- Death recall is a single, brief render: snap down to a small scale, perform one deterministic damped size wobble while fully bright/red, then follow an eased arc toward the Command Table while fading. Never add random per-frame jitter or a second echo/model layer.
- Spinosaurus breach has a client prediction grace state so crossing the waterline cannot briefly revert it to swim/land. Mounted land attacks predict the same aim locally before the authoritative packet arrives. The first-person camera and rendered rider share one persistent animated `whereplayersits` sample with the same forward fallback as the server-side passenger/hitbox anchor; it never expires to the vanilla middle seat.
- Albino pupils use per-species UV masks. Spinosaurus currently has manually masked open-eye pupils until the authored blink texture arrives.
- Automated checkpoint on 2026-08-23 after the compact-depot/breach pass: unit/build passed; all 44 required GameTests passed; the client applied `CameraMixin`, loaded GeckoLib resources, and reached the menu without a fatal/mixin error. Exact rider/camera feel, depot pixels, hatch scale, and death wobble still require the normal human in-game pass.
- Automated checkpoint on 2026-08-23 after adopting the authored Spinosaurus `whereplayersits` locator: pure unit/build passed, all 44 required GameTests passed, and the rebuilt client loaded the updated model/animation resources without a fatal or mixin error. Ground support and aquatic jitter still require the normal human visual pass.
- Automated checkpoint on 2026-08-23 after the Spinosaurus support/surface pass: unit/build passed and all 45 required GameTests passed. The rider attachment is three model pixels lower for Normal and Huge, mounted land step height is 2.05 blocks before genetic scale, terrain pitch samples front/rear support, and only the unmounted AI uses restrained eye-level surface buoyancy. The rebuilt client loaded all ten GeckoLib models/animations without a fatal or mixin error; exact rider height, terrain feel, and waterline appearance still require the normal human in-game pass.
- Automated checkpoint on 2026-08-23 after the active-roster/workstation-defense pass: unit tests and full build passed; all 47 required GameTests passed. Coverage includes flat-area heavy sleeping, worker recovery after a nonlethal owner hit, collision-aware workstation reach, continuous energy work, Processor and chest transport, Ancient Furnace work/progress state, unmounted Spinosaurus surface buoyancy, Pteranodon takeoff/flight, same-owner Parasaurolophus mood support, both turret behaviors, observer/piston energy behavior, and ownership persistence. The rebuilt client applied Primeval Works mixins and loaded all ten GeckoLib models/animations; the dedicated NeoForge server reached `Done` with recipes and advancements loaded and no mod exception. Hatch-card spacing, exact furnace-fill pixels, large-dinosaur work stance, unmounted Spinosaurus waterline appearance, and turret screen feel still require the normal human visual pass.
- Automated checkpoint on 2026-08-23 after the authored Spinosaurus/turret-tracking pass: the new model, six animations, and all six 512x512 visual-state atlases loaded; unit tests and full build passed; all 47 required GameTests passed, including powered turret defense and Processor transport. The integrated client joined `New World`, loaded GeckoLib and JEI, and logged no render/server error or exception. The dedicated server loaded 1,535 recipes and 1,646 advancements and reached `Done`. Exact layered-beam appearance, turret head alignment, revised worker stance, and the restored no-lean Spinosaurus ground rider pose still need the normal human visual pass.
- Automated checkpoint on 2026-08-23 after the turret/stamina pass: unit tests and full build passed; all 47 required GameTests passed on a clean generated test world. Dart and the energy-only beam turret acquisition/fire are exercised independently, and the energy test clears the transient binding to prove reload self-healing. Spinosaurus land sprint has synchronized/persisted stamina and a HUD bar. Exact beam layering, turret aim, Dart palette, and land-sprint feel still require the normal human in-game pass.
- Automated checkpoint on 2026-08-24 after the live turret-energy diagnosis: unit tests/full build passed and all 47 required GameTests passed. The regression test now leaves each connected turret idle for 25 ticks, proves the reserve does not drain, then spawns separate Creepers and requires independent turret acquisition and attack. Ancient Furnace, Processor, and Incubator also release their demand while they have no valid work. A currently running user client still had the previous classes loaded, so the rebuilt behavior requires a client restart and a normal human in-world check before release.
- Automated checkpoint on 2026-08-24 after the workstation/base-isolation pass: small transporters use a collision-clear approach point whose interaction reach overlaps pathfinding tolerance, repeated Processor transport passed 10/10, and base inventory indexing assigns each container to the nearest loaded Command Table. Crafting from base storage passed 10/10 with adjacent test bases, preventing cross-base ingredient theft. The unmounted Spinosaurus surface test now force-loads its pool instead of mistaking an unticked entity's spawn velocity for buoyancy.
- Automated checkpoint on 2026-08-24 after the base-defense boundary pass: both defensive turrets only acquire hostiles inside their connected Command Table's nearest-base cell, so adjacent multiplayer bases cannot steal targets or energy demand from one another. The turret defense test passed 10/10, the complete 47-test GameTest suite passed, and unit tests/full build passed after production screen classes replaced the old prototype filenames. The release JAR contains no docs, Blockbench sources, or prototype screen classes.
- Automated checkpoint on 2026-08-24 after the Magic Turret conversion: the original compact 16x16-texture turret silhouette is retained with iron, blackstone, and amethyst materials. The turret consumes 5 E/S only while targeting, deals 16 direct magic damage at 24-block range, and uses synchronized purple/white beam geometry plus brief shot particles. Registry aliases migrate saved `laser_turret` blocks, items, and block entities to `magic_turret`; the content-registration GameTest proves all three mappings. Unit/full build passed, all 47 required GameTests passed, and the client loaded the restored models and renderer without a missing-model, texture, or mod exception. Exact beam color and turret aim still require the normal human in-world visual pass.
- Automated checkpoint on 2026-08-24 after the egg, hunger, and rapid-beam pass: wild hatches leave one size-weighted stack of 1-3 Fossil Fragments; all work specialties share one persisted hunger-drain clock; close Spinosaurus targets can still be bitten and periodically receive a small clearing shove; and the Magic Turret delivers four five-damage contacts five ticks apart through a cooldown-bypassing custom damage type. Albino open/blink variants remain independent while authored Pteranodon and Spinosaurus saddle pixels bypass recoloring. The Enhanced Rail is absent from content and progression but old saves migrate its block/item ID to vanilla Powered Rail. Pure tests/full build passed, all required GameTests passed, the integrated client loaded all ten GeckoLib model/animation sets, and the dedicated server reached `Done`. Food Box panel composition, saddle preservation, and close-target shove feel still require the normal human in-world visual pass.
- Automated checkpoint on 2026-08-24 after the expedition/Spinosaurus ground pass: the Primordial Frontier uses the explicit two/three/four-star 92/64/12% base risk curve and 120/64/34-minute base duration curve; Huge/Albino multipliers identically affect the server timer, outcome roll, and planner copy. One-star access is rejected by both UI and packet validation. Spinosaurus ground pitch eases from front/rear support with a 12-degree visual cap, and a short forward-only grace window preserves horizontal sprint speed across one-block drops without enabling cliff gliding. New 16x16 silk and four meat textures are imported. The Bayonet is absent from playable content and old stacks migrate to the Primordial Sword. Pure tests/full build passed, all 48 required GameTests passed, the integrated client entered `New World` with JEI and GeckoLib loaded, and the dedicated server reached `Done`; no new crash report or fatal/resource/network error appeared. Exact new item pixels, planner copy, and mounted Spinosaurus incline/drop feel still require a short human visual pass.
- Automated checkpoint on 2026-08-24 after the Command Table alignment pass: the four action labels use their individually tested authored offsets, the upgrade ripple and its dead ellipse helpers are gone, and newly opened/unlocked branches spring-slide from their parent while the purchased node settles. Upgrade descriptions and slotted item requirements have separate bounds. Unit tests/full build passed and the integrated client loaded the updated screen with no fatal, resource, or mixin error; exact label and cost-slot pixels still require the normal human in-game glance.
- Automated checkpoint on 2026-08-25 after correcting the Command Table feedback direction: the prior label correction was inverted and is superseded by the `(+4,+2,+1,-1.5)` horizontal and `-1` vertical offsets recorded above. A pure regression rule proves repeat ranks cannot replay an already-visible branch extension. Upgrade confirmation now combines the retained child slide with a localized gold node glow and level-up/chime; all nine Primeval Works screens use the shared open/close/click sound layer. Unit tests/full build passed, the client loaded the updated classes and initialized OpenAL without a mod error, and exact alignment/glow/audio feel still require the normal human pass.
- Automated checkpoint on 2026-08-25 after the final sound/lifecycle audit: every positional Primeval Works sound is routed through an explicit 5-10-block recipient radius, with dinosaur reach scaling inside that limit and machine/UI feedback remaining local. Login restores only the saved active crew in the claimed Command Table's dimension, loads its last-known/base chunks before fallback, preserves complete work orders and slot order, and never pulls reserve dinosaurs into empty slots. A lethal companion whose owner is offline persists a safe pending-recovery transaction and writes a clean recovery snapshot when the owner returns. Unit tests/full build passed, all 49 required GameTests passed (including relogged active automation without depot auto-fill), the integrated client entered `New World` and shut down cleanly, and the dedicated server reached `Done` with no Primeval Works exception.
- Automated checkpoint on 2026-08-25 after the restrained-audio/individual-recall pass: Primeval Works now deliberately emits only quiet UI feedback, distance-limited heavyweight footsteps, and a rider-local Elytra-style Pteranodon airflow loop while airborne; machine, egg, turret, dinosaur-vocal, work, eating, and sleep cues are silent. Right-clicking an available active dinosaur in the Command Table opens one compact `CALL BACK` action that recalls only that companion without changing crew order or losing its work assignment. Unit tests/full build passed, all 50 required GameTests passed (including selected-only recall and assignment persistence), the integrated client joined `New World` with GeckoLib and JEI initialized, and the dedicated NeoForge server reached `Done` without a Primeval Works exception. Final volume and pitch feel still require the normal human listening pass.
- Automated checkpoint on 2026-08-25 after the animation-audio/config/multi-target pass: empty skill-tree dragging is silent; heavyweight contacts are authored GeckoLib sound keyframes with a configurable 5.5-block steep falloff; unmounted Spinosaurus walk playback defaults to 70% of the exported clip; and mounted airborne Pteranodon wind now starts silently and continuously follows real speed. Work orders accept and persist multiple compatible stations, retain their round-robin cursor across reload, and the energy GameTest requires both selected turbines to receive repeated work without violating worker uniqueness. The native Mods configuration screen exposes client feel settings while the synchronized per-world server TOML owns gameplay balance. All 53 unit tests and all 50 required GameTests passed, the full build passed, the client initialized OpenAL and loaded all ten GeckoLib model/animation sets, and the dedicated server loaded/watched `primevalworks-server.toml` and reached `Done` without a Primeval Works exception. Exact contact timestamps, volume, and wind pitch still need the normal human listening pass.
- Automated checkpoint on 2026-08-25 after the incubator/turbine/Spinosaurus feel pass: heavyweight contacts use nearest collision-bound distance and a stronger close falloff without extending the 5.5-block hard limit; Premium Egg Incubator previews fit each egg class inside the glass without continuous rotation; idle roam eligibility increased from one in forty to one in thirty; and Spinosaurus begins its authored bite through the final facing correction instead of parking before the animation. The Water Turbine is now a water-retaining 3x3 cog with eight animated paddles and a tested three-cell submerged-row rule; breaking it restores the retained water. All 55 unit tests, the full build, and all 50 required GameTests passed. The integrated client loaded the ten GeckoLib model/animation sets with no missing turbine model or resource error, and a normal dedicated server loaded/watched `primevalworks-server.toml` and reached `Done`. Exact footstep volume, incubator framing, cog art, and revised attack feel still need the normal human in-world pass.
- Automated checkpoint on 2026-08-26 after the Velociraptor/material-art import: the authored Velociraptor source, model, blink atlas, and six supplied clips are wired under the stable `velociraptor` asset ID; its preview and collision use the export's 0.77-by-1.55-block body, procedural motion uses the authored bones, and bite damage lands at the supplied contact timing. Four revised meat sprites and authored Fossil Fragment, Hardwood, both bone, Pteranodon Wing Fragment, and Tyrannosaurus Tooth icons replace their prior art/vanilla stand-ins without changing registry IDs. Unit/resource tests and the full build passed; a clean rerun passed all 51 required GameTests after one unrelated Stegosaurus furnace timing flake in the first run. The client loaded 11 GeckoLib model/animation sets and every item resource without an error, and the normal dedicated server loaded the synchronized server config and reached `Done`. Exact Velociraptor world scale, mouth-held cargo position, UI framing, albino pupils, and animation blend still require the normal human in-world pass.
- Automated checkpoint on 2026-08-26 after the Raptor/genetics/egg-pool pass: Velociraptor Pursuit Instinct now builds sustained acceleration, drives real-speed run playback, preserves velocity through a stronger airborne pounce, and lands damage at the authored contact frame. Birth quality lightly changes work speed, hunger interval, and work-mood drain; quality, level, Huge, and Albino scale each functional species passive. The exact eight-species roster owns every registered dinosaur/spawn egg/hatch path, and the three rarer weighted wild-egg pools cover that roster with Velociraptor the rare result wherever it appears. Depot hover cards show mutations, and the Work Planner's retracting left rail attaches to the physical screen edge across aspect ratios without rescaling the rest of the authored UI. All 65 unit tests, the full build, and all 51 required GameTests passed. The integrated client loaded all 11 GeckoLib model/animation sets, entered `New World`, and accepted normal commands without a Primeval Works crash; the old dev world's removed-species statistics/advancement records emitted migration warnings and were safely ignored. Exact Raptor acceleration, pounce weight, run playback, depot card spacing, and cross-resolution planner pixels still require the normal human in-game pass.
- Automated checkpoint on 2026-08-26 after restricting Raptor Pursuit: ordinary moving no longer builds momentum or selects the run clip. A synchronized pursuit flag is true only while the server is routing the Raptor through an actual transport leg, chasing a live target, or finishing a committed pounce; waiting, roaming, eating, base return, and non-transport work retain the original walk. All 66 unit tests and all 52 required GameTests passed, including independent ordinary-walk and combat-pounce regressions.
- Automated checkpoint on 2026-08-26 after the filtered-work/Raptor continuity pass: Transport and Crafting item pickers use one-click selection and close cleanly; Fire keeps its intentional multi-slot drag workflow. Saving stays in the planner until the server confirms the exact assignment, so a rejected order no longer closes the screen or loses its draft. Transport routes reject any inventory selected as both source and destination, filtered-route coverage proves unselected cargo stays put, and all saved work/depot/login/multi-target/machine regressions remain green. Velociraptor turning now uses a continuous nonzero speed curve instead of the shared hard stop thresholds; its pursuit gait has a short synchronized hold and smoothed real-speed playback so brief path/packet fluctuations do not restart the walk/run clips. All 68 unit tests, all 52 required GameTests, and the full build passed.
- Automated checkpoint on 2026-08-26 after the Raptor steering/authored-turret pass: active Velociraptor pursuit has one steering authority, blends its preserved momentum toward the live route with a speed-sensitive response, skips the generic body-facing pull, and no longer restarts transport navigation every ten ticks. The authored 128x128 turret model, six-frame firing atlas, jaw animation, target-tracking head, and wall-clipped layered beam are live. Spinosaurus Head is an exact `head2`-subtree trophy block, drops on 8% of direct player Spinosaurus kills or an independent 1.8% Primordial Frontier roll, and gates the hard turret recipe with compressed Ancient Metal, Redstone Blocks, an Ancient Spell Stone, Magic Shards, and a Compressed Core. Combat GameTests now discard their actors on success so surviving hostiles cannot leak into another workstation test. All 72 unit/resource tests, all 52 required GameTests, and the full build passed; the integrated client loaded all 13 Gecko models and 13 animation files with no mod/resource exception. Exact turret pivot, muzzle, animated atlas, trophy framing, and Raptor run line still require the normal human in-world pass.
- Automated checkpoint on 2026-08-26 after restoring the Laser Turret and importing the Ancient Barrel: `laser_turret` is again the canonical block/item/block-entity ID, while retired `magic_turret` saves migrate forward. The endgame technology recipe uses the Spinosaurus Head, Laser Observer, Redstone Blocks, Ancient Metal, Compressed Ancient Metal, and a Compressed Core with no spell ingredients. The authored head yaw correction is inverted, beam feedback is red/white, and the item uses the same Gecko model as the placed turret instead of the stale hotbar placeholder. Ancient Barrel uses its exact 64x64 closed/open art, directional blockstates, a 54-slot container, and opener-driven visual switching; normalized-UV coverage prevents another missing-model bake. All 73 unit/resource tests, the full build, and all 52 required GameTests passed; the integrated client loaded all 13 Gecko models/animations with no missing-model, texture, resource, or Primeval Works exception. Exact turret aim direction, hotbar framing, and barrel pixels still require the normal human in-world pass.
- Automated checkpoint on 2026-08-26 after the turret-facing and inventory-model correction: the prior inverted head-yaw change and special Gecko item renderer are superseded. Head and beam again use the same signed yaw; the placed turret retains its animated Gecko hierarchy, while its inventory form is a normal 23-cube vanilla block model with the authored atlas. Spinosaurus Head now uses the turret source's 21 `head`/`mouth` cubes as one directional vanilla block model, so its placed and inventory forms share identical geometry and texture. The obsolete trophy Gecko renderer, actual-dinosaur `head2` export, generic trophy item placeholder, and special turret item classes are removed. All 73 unit/resource tests and the full build passed; a clean rerun passed all 52 required GameTests after one unrelated Pteranodon takeoff-timing flake, and the integrated client baked both normal block items and loaded the remaining 12 Gecko models without a missing-model, texture, resource, or Primeval Works exception.
- Automated checkpoint on 2026-08-26 after the authored Processor/two-phase Wind Turbine pass: the directional Processor uses its authored static 64x64 cube, and both Wind Turbine phases use the authored animated model with separate 128x128 base/upgraded atlases. The basic phase produces 60% of the former wind output; processing it with ordinary fuel and a Pteranodon Wing Fragment yields the persistent full-output phase. Wing Fragments remain direct Pteranodon drops but expedition access is restricted to Predator Run and Primordial Frontier. Both phases share multiblock placement, assignment, rendering, energy-network, loot, and save-safe block behavior. All 74 unit/resource tests, the full build, and all 53 required GameTests passed; the integrated client loaded 12 Gecko models and 13 animations without a missing resource or mod error, and the dedicated server reached `Done`. Exact Processor face orientation and turbine rotor/atlas pixels still require the normal human in-world glance.
- Automated checkpoint on 2026-08-27 after the global block-presentation/thin-panel/turbine-activity pass: all 19 placeable block items had a preview path, every visible dinosaur and animated flat-panel machine used alpha-cutout back-face culling, and Wind/Water rotors received a synchronized heartbeat only from a valid worker at that exact station. The later non-cube item pass supersedes the blanket full-cube presentation rule from this checkpoint.
- Automated checkpoint on 2026-08-27 after the non-cube block-item correction: all 19 placeable block items have an exact expected preview-model mapping. Wind, Upgraded Wind, and Water turbine items use their authored Gecko geometry with cutout culling and no item animation controller; the obsolete copper/cube placeholders are gone. Laser Turret and Spinosaurus Head keep their complete 23/21-cube authored vanilla geometry but use one reviewed compact item-only transform, while all three physical eggs use their own fitted profile. Live hotbar and held-view checks confirmed the authored turbine silhouettes, readable turret/trophy slots, and hand-safe sizing without changing any placed model. All 79 unit/resource tests, all 53 required GameTests, and the full build passed; the integrated client loaded 12 Gecko models and 13 animations without a missing resource, and the dedicated server reached `Done`. The release JAR contains the new render paths and no documentation/source-art files.
- Automated checkpoint on 2026-08-27 after the turbine hotbar-centering correction: a live 16x16 creative-hotbar pass calibrated the Basic Wind, Upgraded Wind, and Water silhouettes under Minecraft's GUI projection. Each variant owns its tested left/up position; the two Wind atlases currently share one value while Water differs. These translations are restricted to `ItemDisplayContext.GUI`, leaving held and placed transforms untouched. All 79 unit/resource tests and the full build passed.
- Automated checkpoint on 2026-08-27 after the follower/Dino Whistle field-automation pass: Home, Stay, and Follow are authoritative saved commands with hover explanations; Field Command upgrades the follower cap from one to three; capable followers defend the owner; and the whistle provides server-validated Quarry, Lumber, Harvest, and loose-item Collect orders with single, connected, area, repeat, and 16-85-block leash controls. Field assignment cannot break machines, block entities, protected blocks, unloaded chunks, or bypass follower limits, and cargo/state are normalized at every roster boundary. Energy assignment now outlines the authored full 3x4x1 Wind and 3x3x1 Water machines instead of obsolete part cubes. All 83 unit/resource tests, all 55 required GameTests, and the full release build passed; the integrated client joined `New World` with all 12 Gecko models/13 animations and JEI loaded, then shut down cleanly, and the normal dedicated server loaded the synchronized Primeval Works config and reached `Done`. Exact whistle panel pixels, follower path feel around unusual terrain, and the full-machine energy outline remain normal human in-world visual checks.
- Automated checkpoint on 2026-08-27 after the field-specialty/UI correction: only the five explicit species specialists can accept one whistle field role; all picker and assignment checks use the same server rule. The Jobs field row and whistle/picker/HUD panels use the dark palette, full-size bold hierarchy, colored hover state, settling wobble, spring opening, and resolution-fit transform, while the companion screen returned to one Home/Stay/Follow cycling button. All 85 unit/resource tests, all 55 required GameTests, and the full release build passed. The integrated client initialized the GUI atlas, all 12 Gecko models/13 animations, JEI, and audio without a Primeval Works exception; the normal dedicated server loaded the synchronized config and reached `Done`. Exact pixel taste and hover feel remain a normal human in-world visual check.
- Automated checkpoint on 2026-08-27 after restoring the authored whistle UI language: the raw code-drawn whistle, follower-picker, held-HUD, and field-row frames are superseded by the shared two-pixel nine-slice of the authored 86x14 `space.png`. Config and picker openings now use the Companion screen's long floating-point spring slide, resolution fit, smoothed parallax, inverse hit transform, and settling hover wobble; the held HUD uses the same bubble with a fully retracting eased slide. All 87 unit/resource tests and the full build passed. The integrated client joined `New World`, initialized the GUI atlas, all 12 Gecko models/13 animations, JEI, and audio without a Primeval Works exception; exact visual taste and cursor feel remain the normal in-world human pass.
- Automated checkpoint on 2026-08-27 after the whistle interaction rework: the prior dark dashboard and simultaneous option-card grids are superseded. Configuration is now one authored warm `space.png` bubble each for Order, Target, Run, Range, and contextual help; clicking a setting cycles it just like the established command control, while range remains a direct slider. Picker entries, held HUD, and the Jobs field row use the same warm authored frame without dark-card overlays. Config/picker openings use the established Companion submenu popup spring with restrained floating-point parallax, inverse hit testing, and transient hover wobble that settles. All 88 unit/resource tests and the full build passed; the client initialized the GUI atlas, all 12 Gecko models/13 animations, JEI, and audio without a Primeval Works exception. Exact pixels and interaction feel still require the normal human in-world pass.
- Automated checkpoint on 2026-08-27 after the compact mode-aware whistle and authored Water Turbine pass: the whistle config matches the Command Table's 195x148 footprint, its range uses the supplied 12x12 button, its held HUD is 116 pixels wide, and it always suppresses vanilla attacks. Quarry, Lumber, Harvest, and Collect now validate and explain different targets; Collect persists an optional searchable item filter across follower reload. The former follower dashboard is gone—live dinosaur models spring into authored 18x18 hotbar frames over the marked block. Home/Stay/Follow keeps one cycling control with only a subtle state wash and no colored outline. The supplied 256x256 Water Turbine geometry, atlas, four-second rotor animation, and Blockbench source replace the placeholder; world animation still requires the exact active-worker heartbeat, expanded render bounds prevent edge culling, and a per-variant item scale keeps the larger model inside hotbar/held views. All unit/resource tests, the full build, and all 55 required GameTests passed; the integrated client initialized the GUI/item atlases and loaded all 12 Gecko models/13 animations without a Primeval Works exception. Exact whistle pixels, live preview framing, and Water Turbine inventory/world centering remain the normal human in-game pass.

- Automated checkpoint on 2026-08-27 after the passive-first whistle rework: right-click configuration and the world-side whistle HUD are gone. Hovering the whistle in the player's inventory and holding Shift fills the established 16-frame work meter before opening the 195x148 authored panel. Quarry and Lumber are the only marked roles; Quarry either follows a connected vein or mines the selected block type inside two corners, while Harvest and Collect continuously rescan around their assigned follower. Collect uses a smoothly extending search panel over the player's real inventory and a dragged filter template without consuming the item. Once/Loop was removed, saved legacy settings normalize safely, and all assignments are revalidated server-side. All pure tests and the full build passed; a clean rerun passed all 57 required GameTests, including passive crop replanting and filtered loose-item recovery. The integrated client initialized LWJGL, OpenAL, and the sound engine without a Primeval Works startup exception. Exact tooltip spacing, search-panel motion, and live follower framing still require the normal human in-game visual pass.
- Automated checkpoint on 2026-08-27 after the whistle/persistence/follower hardening pass: the vanilla item tooltip is restored, either physical Shift key reliably opens configuration from wrapped player-inventory slots, and the held item has one compact authored right sidebar. Field assignments are durable orders rather than a transient Follow flag: Home/Stay, NBT reload, chunk unload, logout, depot storage, and reactivation suspend and resume unfinished work without deleting its settings. Spinosaurus uses amphibious navigation and its idle surface-float behavior yields to real movement intent; Pteranodon followers use powered catch-up flight; Velociraptor followers use pursuit speed only while genuinely catching up; all followers use adaptive repathing and stall recovery. Spinosaurus dismount clears breach/controller state and cannot leave an airborne swim animation latched. All 93 unit/resource tests and all 59 required GameTests passed, the full build succeeded, the integrated client joined `New World` without a Primeval Works/resource/protocol failure, and the dedicated server reached `Done` with the synchronized server config.
- Automated checkpoint on 2026-08-27 after the compact right-click whistle and navigation-arbitration pass: the Shift timer and permanent sidebar are superseded by immediate right-click configuration from either the held whistle or its inventory slot. The authored popup is a resolution-safe 164x110 card; detailed explanations live in hover tooltips, and Collect alone opens a compact 178x112 searchable real-inventory picker. Follow pursuit and field duty now have one explicit navigation owner, preventing the two goals from canceling each other's paths; idle/passive followers catch up promptly, active short collection legs use a stable target, Pteranodon enters catch-up flight sooner, and swimming Spinosaurus uses smooth direct aquatic pursuit instead of surface-idle control. Follow remains active overnight while Home/base workers retain normal sleeping behavior. Regression coverage includes saved roster/login/expedition/follower orders, passive crop and loose-item work, land and aquatic Spinosaurus catch-up, Pteranodon flight, multi-station work, and the compact no-sidebar UI contract. All 93 unit/resource tests, the full build, and all 61 required GameTests passed. A live integrated client opened the compact whistle card in `New World`, loaded JEI/GeckoLib/audio, and saved/shut down without a mod, resource, render, or protocol error; a dedicated NeoForge server loaded/watched `primevalworks-server.toml` and reached `Done` without a Primeval Works exception.
- Automated checkpoint on 2026-08-27 after the readable whistle/automatic-Quarry correction: the cramped 164x110 card and separate homemade picker are superseded by a `196x142` nested-bubble setup and `198x128` Work Planner-style inventory overlay. Descriptions are wrapped at readable scale, filters use an authored plus slot plus the same spring paper grid as base work, and opening uses the Command Table popup timing. Quarry no longer enters the broken mark/corner flow; it is a continuous bounded nearby-mining duty assigned directly to a compatible follower. Base-work rows and controls are visibly locked while Follow is active, the field row explains its Follow/whistle requirement, and the server independently rejects forged base-work assignment packets for followers. Unit/resource tests and all 61 required GameTests passed, including the new automatic-Quarry world interaction regression. A rebuilt integrated client then opened the popup, opened and typed into the inventory filter, selected a real item, and visually exercised the closing spring; the paper panel stays behind its non-alpha item icons until they disappear, and the latest client log contains no Primeval Works, render, resource, or protocol error.
- Automated checkpoint on 2026-08-27 after restoring marked Quarry and the creative cargo catalogue: the automatic-Quarry behavior above is superseded. Left-click marks one block for Connected mining or two corners for Area mining; Connected follows the marked block type, while Area clears every safe breakable block in the bounded volume. Collect's plus slot now opens a non-consuming creative-style item catalogue, with the main card and catalogue separating through the same frame-rate-independent spring used by the Work Planner. Companion Jobs becomes a large Call Back action when both owner and dinosaur are outside the base, and base jobs remain inaccessible there. Spinosaurus closes farther before committing attacks while retaining its close-target shove. All 94 unit/resource tests, the full release build, and all 62 required GameTests passed. The integrated client joined `New World` with all GeckoLib resources and JEI loaded, and the dedicated NeoForge server reached `Done` without a Primeval Works, resource, render, or protocol exception. Exact hover motion, text baselines, turbine hotbar centering, and the new two-corner workflow still require the normal human in-game visual pass.
- Automated checkpoint on 2026-08-27 after importing the reusable UI crop atlas: the exact `427x240` `ui_crop.png` now supplies fifteen isolated paper and dark-theme primitives through one alpha-aware, border-preserving crop helper. The Dino Whistle uses its paper bubbles, inset spaces, rules, catalogue panel, slots, range track, and knob; the Work Planner catalogue shares the same panel/slot crops so both item searches retain one visual language. All 95 unit/resource tests and the full release build passed. The integrated client joined `New World`, loaded Primeval Works resources, GeckoLib, and JEI without a missing-texture, render, resource, or protocol exception. Exact atlas styling and hover motion still require the normal human in-game visual pass.
- Automated checkpoint on 2026-08-28 after the authored material/Observer pass: live progression replaces both Ancient Spell Ingot IDs with `magic_shard_fragment` and the animated six-frame `magic_shard`, while registry aliases migrate either old ID. Primordial Frontier supplies fragments; the Processor binds one fragment with a Fossil Fragment; all late-game recipes use the finished shard. The authored Ancient Metal Ingot and Nugget sprites are live with reversible 9:1 crafting. The Laser Observer uses its exact 64x64 atlas UVs, separate powered texture, and all six facing states instead of the directionless placeholder. Fixed-width Whistle and Work Planner labels now fit their actual authored spaces, including catalogue counts and split rule labels/values, and the Water Turbine GUI-only presentation moved farther left/up without touching held or world transforms. All 99 unit/resource tests, the full release build, and all 62 required GameTests passed. The integrated client baked the new block/item atlases and loaded all Gecko resources without a Primeval Works model, texture, animation, or protocol error.
- Automated checkpoint on 2026-08-28 after the release movement/turbine cleanup: `magic_shard_fragment` uses the supplied Ancient Spell Nugget sprite exactly; reinforced piston content, recipes, mixins, assets, tooltips, and tests are removed. The Spinosaurus Head retains the exact Laser Turret geometry but is grounded with directional collision. Large-dinosaur movement no longer fights itself with zero-speed body-turn gates: head motion leads, the body follows continuously, idle roaming is rarer, followers run/fly/swim to catch up, and safe teleport is reserved for 100-block emergencies. Quarry now previews its first corner and selected volume, opens its follower choice only after the second corner, and synchronizes that area for the persistent held-whistle outline. Call Back immediately returns the dinosaur and changes it to Home. A directly worked Water Turbine links only its nearest two available submerged neighbors within five blocks at 55% and 35% output without recursive chains. Upgrade hover copy quickly fades to the established dark-brown ink. All 103 unit/resource tests, the full release build, and all 62 required GameTests passed. The integrated client initialized OpenAL, baked the GUI/block/item atlases, and loaded 12 GeckoLib models/13 animations without a Primeval Works resource or protocol error; the dedicated NeoForge server loaded the synchronized config and reached `Done`.
- Automated checkpoint on 2026-08-28 after the final water/field-work release pass: every valid submerged Water Turbine contributes 20% passive output to its nearest loaded base and keeps that baseline when a worker adds full active output. The authored rotor uses a restrained passive speed, switches to full speed under direct or linked work, and renders its two-sided planes without losing the corner face. Its recipe now calls for two Hardwood and exactly two Big Dino Bones. The Spinosaurus Head block uses the full turret-source geometry with fitted item transforms rather than shrinking the placed trophy. Quarry capacity scales from a compact level-one volume to a 16-block/512-block level-one-hundred ceiling, oversized previews turn red with a concrete level requirement, active areas remain visible, and moving stripes distinguish the live selection. Lumber fells a connected tree from the lowest reachable trunk upward after one real work strike. Whistle filters now update already-assigned passive followers, search contents share one smooth reveal, large species receive longer field-work reach, and unmounted Spinosaurus stays level at the water surface when idle. The latest authored Velociraptor open/blink textures are live and its run controller blends in only after real pursuit momentum. Login restoration now preloads active companion chunks and waits for entity integration before recreating a missing snapshot, eliminating the duplicate-UUID warnings reproduced in the old development world. All 105 unit/resource tests and all 62 required GameTests passed; the full build, integrated client, and dedicated server completed without a Primeval Works model, texture, protocol, or startup error.
- GitHub presentation pass on 2026-08-28: the public README now leads judges through a blue internal contents row, gives the Dino Whistle its complete field-command explanation, and omits contributor launch commands and internal verification links from the public pitch. It uses prose instead of bullet-dot feature lists or decorative separators. Generic ground followers now scale walk and run playback with their smoothed chase movement; specialized Pteranodon, Spinosaurus, and Velociraptor curves remain authoritative. The clean release build and all 106 unit/resource contracts passed after this change.

## Detailed companion documents

- `GAME_DESIGN.md` — progression and balance.
- `BACKEND_ARCHITECTURE.md` — authoritative state, reservations, persistence, networking.
- `DETAIL_BIBLE.md` — required polish and feedback loops.
- `ART_ASSET_CONTRACT.md` and `MODELER_BRIEF.md` — model/export/bone/animation contracts.
- `UI_ASSET_MASTER_LIST.md`, `ASSIGN_JOB_UI_PACK.md`, and `UI_STYLE_GUIDE.md` — authored UI and interaction language.
- `CONTENT_CATALOG.md` — registered content and behavior.
- `TESTING.md` — commands and verification workflow.
- `WRITING_STYLE.md` — player-facing copy.
